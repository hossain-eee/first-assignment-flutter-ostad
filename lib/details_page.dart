import 'package:flutter/material.dart';

class DetailsTak extends StatelessWidget {
  const DetailsTak({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Task Management"),
        centerTitle: true,
      ),
    );
  }
}
